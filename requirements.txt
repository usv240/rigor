# Rigor - AI research-integrity referee

# LLM extraction (Qwen via DashScope, OpenAI-compatible)
openai>=1.40
python-dotenv>=1.0

# Deterministic checks (no API key needed)
scipy>=1.11

# PDF ingest
pymupdf>=1.24

# Web API / demo
fastapi>=0.110
uvicorn[standard]>=0.29
slowapi>=0.1.9

# MCP server (expose the checks as tools any agent can call)
mcp>=1.2

# Tests
pytest>=8.0
